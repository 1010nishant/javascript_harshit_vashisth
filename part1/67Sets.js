// Sets (it is iterable)
// store data  
// sets also have its own methods
// No index-based access so that Order is not guaranteed
// Order is not guaranteed
// unique items only (no duplicates allowed)
// const items = ['item1', 'item2', 'item3'];
// const numbers = new Set();
// numbers.add(1);
// numbers.add(2);
// numbers.add(3);
// numbers.add(4);
// numbers.add(5);
// numbers.add(6);
// numbers.add(items);//added as an array
// numbers.add(['value1',"value2"])
//both are added because both array are differnt in perspective of js it doesn't matter element is same because addreas is different
// numbers.add(['value1',"value2"])
// if(numbers.has(1)){
//     console.log("1 is present")
// }else{
//     console.log("1 is not present")
// }
// for(let number of numbers){
//     console.log(number);
// }
// const myArray = [1,2,4,4,5,6,5,6];
// const uniqueElements = new Set(myArray);
// let length = 0;
// Set don't have .length method
// for(let element of uniqueElements){
//     length++;
// }

// console.log(length);

// Use the size property to get the length of a Set - mySet. size . The size property returns the number of values in the Set object

// has()	Returns true if a value exists
//for understanding search on web for more menthods
