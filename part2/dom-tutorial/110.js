// innerHtML 
// The innerHTML property sets or returns the HTML content (inner HTML) of an element.
const headline = document.querySelector(".headline");
// console.log(headline.innerHTML);
// headline.innerHTML = "<h1>Inner html changed </h1>";//it will replace the already define innerHTML and will write under inverted comma
// headline.innerHTML += "<button class= \"btn\"> Learn More </button>" //it will add element in inverted comma with already innerhtml
// console.log(headline.innerHTML);
